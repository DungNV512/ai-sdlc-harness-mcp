// Stub template for scaffolded files. Replace placeholders during
// codegen, then run `dart format .`.
//
// {{SLUG}}      -> snake_case feature slug
// {{Pascal}}    -> PascalCase feature name
//
// Example invocation:
//   sed -e 's/{{SLUG}}/feed/g' -e 's/{{Pascal}}/Feed/g' template.dart

/// {{Pascal}} entity.
class {{Pascal}} {
  /// Creates a new [{{Pascal}}].
  const {{Pascal}}({required this.id});

  /// Unique identifier.
  final String id;
}
